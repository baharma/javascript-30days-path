document.addEventListener('DOMContentLoaded', main);

// Kode document.addEventListener('DOMContentLoaded', main) digunakan untuk menjalankan fungsi main ketika dokumen HTML telah sepenuhnya dimuat.

// Ini berguna untuk memastikan bahwa seluruh dokumen telah dimuat sebelum menjalankan kode JavaScript yang terkait dengan elemen HTML di halaman tersebut. Dengan menunggu hingga dokumen selesai dimuat, kode JavaScript dapat dijalankan dengan benar dan mencegah masalah yang mungkin terjadi jika kode dijalankan sebelum dokumen selesai dimuat.

// Dalam hal ini, fungsi main kemungkinan besar berisi kode JavaScript yang menjalankan logika bisnis atau menambahkan interaktivitas pada elemen HTML di halaman tersebut.